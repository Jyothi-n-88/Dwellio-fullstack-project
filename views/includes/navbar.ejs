<nav class="navbar navbar-expand-md bg-white border-bottom sticky-top shadow-sm">
  <div class="container-fluid px-4">

    <!-- Logo -->
    <a class="navbar-brand brand-logo d-flex align-items-center" href="/listings">
  <i class="fa-solid fa-compass me-2"></i>
  <span>Dwellio</span>
</a>

    <!-- Toggle Button -->
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse"
      data-bs-target="#navbarNavAltMarkup" aria-controls="navbarNavAltMarkup"
      aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarNavAltMarkup">

      <!-- Left Side Links -->
      <div class="navbar-nav">
        <a class="nav-link fw-semibold" href="/listings">Explore Home</a>
      </div>
     <form class="search-wrapper mx-auto position-relative" action="/listings" method="GET">
  
  <input 
    type="text" 
    name="search"
    id="searchInput"
    class="search-input-main"
    placeholder="Search destinations..."
    autocomplete="off"
    value="<%= typeof search !== 'undefined' ? search : '' %>"
  >

  <button type="submit" class="search-btn-main">
    <i class="fa-solid fa-magnifying-glass"></i>
  </button>

  <!-- Suggestions dropdown -->
  <div id="suggestionsBox" class="suggestions-box d-none"></div>

</form>
      <!-- RIGHT SIDE (Airbnb Style) -->
      <div class="navbar-nav ms-auto align-items-center gap-3">

  <a class="nav-link host-btn" href="/listings/new">
    Switch to hosting
  </a>

  <% if(!currentUser) { %>

    <a class="nav-link fw-semibold" href="/signup">Sign Up</a>
    <a class="nav-link fw-semibold" href="/login">Login</a>

  <% }
   else { %>

    <div class="dropdown">
      <button class="btn profile-btn dropdown-toggle" type="button"
        data-bs-toggle="dropdown" aria-expanded="false">
        <i class="fa-solid fa-user"></i>
        <span class="ms-2"><%= currentUser.username %></span>
      </button>

      <ul class="dropdown-menu dropdown-menu-end shadow">
        <li><a class="dropdown-item" href="/profile">Profile</a></li>
        <li><a class="dropdown-item" href="/listings?user=<%= currentUser._id %>">My Listings</a></li>
        <li><hr class="dropdown-divider"></li>
        <li><a class="dropdown-item text-danger" href="/logout">Logout</a></li>
      </ul>
    </div>

  <% } %>

</div>

    </div>
  </div>
</nav>
